import { QuotationsFilterPipe } from './quotations-filter.pipe';

describe('QuotationsFilterPipe', () => {
  it('create an instance', () => {
    const pipe = new QuotationsFilterPipe();
    expect(pipe).toBeTruthy();
  });
});
