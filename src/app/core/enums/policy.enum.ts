export enum EPolicyStatus{
    active = 1,
    expired = 2
}
export enum EPolicyNajmStatus{
    pending = 1,
    failed = 2,
    success = 3,
}

export enum EPolicyTabs{
    active = 1,
    toRenew = 2,
    expired = 3,
    underPublish = 4
}